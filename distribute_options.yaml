variables:
  APP_ENV: "prod"
output: dist/
releases:
  - name: prod
    jobs:
      - name: release-ios
        package:
          platform: ios
          target: app
          build_args:
            export-method: development  # добавляем обязательный параметр