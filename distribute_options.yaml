variables:
  APP_ENV: "prod"
output: dist/
releases:
  - name: prod
    jobs:
      - name: release-ios
        package:
          platform: ios
          target: app   # <<< важно: собираем .app
          build_args:
            # export-options-plist: ios/ExportOptions.plist  # эту строку удаляем